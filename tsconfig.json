{
  "compilerOptions": {
    // モジュール形式
    "module": "commonjs",
    // モジュールの解決方法。AWS Lambdaで利用するならnode。
    "moduleResolution": "node",
    // コンパイル後のターゲット（'es3', 'es5', 'es2015', 'es2016', 'es2017'）
    // 何も指定しない場合のデフォルト値は'es3'
    "target": "es2017",
    // SourceMapを出力する
    "inlineSourceMap": true,
    "inlineSources": true,
    // 出力先のディレクトリ
    "outDir": "dist",
    // コンパイル時にコメントを削除する
    "removeComments": true,
    // noImplicitAny, strictNullChecks, noImplicitThis, alwaysStrict strictFunctionTypes strictPropertyInitialization を同時に有効にする
    "strict": true,
    // 未使用なローカル変数はエラーにする
    "noUnusedLocals": true,
    // 到達しえないコードがある場合はエラーにする
    "allowUnreachableCode": false,
    // 到達しえないラベルがある場合はエラーにする
    "allowUnusedLabels": false,
    // 暗黙的な undefined を返す事を禁止する
    "noImplicitReturns": true,
    // Decoratorsを有効にする
    "forceConsistentCasingInFileNames": true,
    "lib": [
      "es2015",
      "es2016",
      "es2017"
    ],
    "types": [
      "node"
    ]
  },
  "exclude": [
    "node_modules"
  ]
}
